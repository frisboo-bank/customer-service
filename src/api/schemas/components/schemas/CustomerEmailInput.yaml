type: object
additionalProperties: false
required:
  - email
properties:
  email:
    type: string
    format: email
    maxLength: 320
  isPrimary:
    type: boolean
    description: Mark one email as primary if multiple are provided.
    default: false
